#main-content{
    height: 100%; 
    background:#fff;
    box-shadow: 1px 0 20px rgba(0,0,0,0.05);
}
.page-wrapper {
    /* position: inherit;
    margin: 0 0 0 240px; */
    padding-bottom: 5px;
    min-height: calc(100vh);
    background: #fff;
    overflow: hidden;
}